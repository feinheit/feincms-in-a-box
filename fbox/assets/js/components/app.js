/**
 * Haupt Entry-Point für die Fahrzeug-Assistenten-Suche.
 */
import 'babel-core/polyfill';
// import React from 'react';

/*
import { Router, Route } from 'react-router';

import FAS from './fas';
import CompareList from './compare/compareList';


let NoMatch = React.createClass({
  render: function() {
    return (<div>No Route Match</div>);
  }
});


// Load the app
let container = document.getElementById('fas-search');
React.render((
  <Router>
    <Route path="/" component={FAS} />
    <Route path="compare" component={CompareList} />
    <Route path="*" component={NoMatch} />
  </Router>
  ), container
);
*/
